using UnityEngine;

public class Scoring : MonoBehaviour
{
    // Placeholder cho logic điểm (nếu cần mở rộng sau)
    void Start()
    {
        
    }

    // Update mỗi frame (không dùng hiện tại)
    void Update()
    {
        
    }
}
